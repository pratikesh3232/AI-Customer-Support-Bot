# AI Customer Support Chatbot

An AI-powered customer support chatbot built with LangChain and RAG. It answers questions grounded in your own knowledge base (FAQs, return policies, product manuals) and remembers the full conversation history across turns.

---

## Demo

> User: "What is your return policy?"
> Bot: "You can return items within 30 days of delivery. The item must be unused and in original packaging... [return_policy.txt]"

> User: "What if the item is damaged?"
> Bot: "If your product arrives damaged, contact us within 48 hours with photos... [return_policy.txt]"

The bot remembers previous messages — so follow-up questions work naturally.

---


## Features

- **RAG-powered answers** — retrieves relevant chunks from your knowledge base before answering
- **Conversation memory** — remembers the full chat history so follow-up questions work naturally
- **Multi-document KB** — supports FAQ `.txt`, return policy `.txt`, and product manual `.pdf`
- **Source citations** — every answer references which document it came from
- **Streamlit chat UI** — native `st.chat_message` bubbles with `st.chat_input`
- **One-time KB build** — embed your docs once, reuse the FAISS index every session
- **Cached retriever** — FAISS index loads once per session with `@st.cache_resource`

---

## Tech Stack

| Layer | Technology |
|---|---|
| LLM | GPT-4o-mini (OpenAI) |
| Embeddings | OpenAI `text-embedding-ada-002` |
| Vector store | FAISS |
| Memory | LangChain `ConversationBufferMemory` |
| RAG chain | LangChain LCEL |
| Document loaders | `TextLoader`, `PyPDFLoader` |
| Chunking | `RecursiveCharacterTextSplitter` |
| UI | Streamlit |

---

## Project Structure

```
AI-Customer-Support-Chatbot/
├── app.py                  # Main Streamlit chatbot app
├── build_kb.py             # One-time script to build FAISS knowledge base
├── faiss_kb/               # Saved FAISS index (generated by build_kb.py)
├── docs/
│   ├── faq.txt             # Frequently asked questions
│   ├── return_policy.txt   # Return and refund policy
│   └── product_manual.pdf  # Product manual (TechNova X200)
├── requirements.txt        # Python dependencies
├── .env                    # API keys (not committed)
└── README.md
```

## Knowledge Base

The chatbot's knowledge comes from three documents in the `docs/` folder:

| File | Contents |
|---|---|
| `faq.txt` | Q&As covering payments, shipping, cancellations, and support |
| `return_policy.txt` | Full 8-section return, refund, and exchange policy |
| `product_manual.pdf` | TechNova X200 wireless headphones — specs, controls, pairing, troubleshooting |

To add your own documents, place them in `docs/` and update the `loaders` list in `build_kb.py`, then re-run `python build_kb.py`.

---

## How RAG Works

```
User question
    │
    ▼
OpenAI Embeddings converts question to vector
    │
    ▼
FAISS finds top-4 most similar chunks
    │
    ▼
Chunks are formatted with [source] tags
    │
    ▼
Injected into the system prompt as {context}
    │
    ▼
LLM generates answer grounded in those chunks
```

If the answer is not in the knowledge base, the bot replies:
> "I don't have that information — please contact our support team."

---

## Requirements

```
langchain
langchain-openai
langchain-community
langchain-text-splitters
faiss-cpu
pypdf
streamlit
python-dotenv
```

---

## Known Limitations

- Memory resets when the page is refreshed (in-session only)
- FAISS is in-memory — index must be rebuilt if you add new docs

---

## Author

**Pratikesh Howale**
- LinkedIn: [linkedin.com/in/pratikesh-howale-32246418a](https://linkedin.com/in/pratikesh-howale-32246418a)
- GitHub: [github.com/pratikesh3232-github](https://github.com/pratikesh3232-github)

---

## License

MIT License — free to use and modify.
